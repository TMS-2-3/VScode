(() => {
  "use strict";

  // Deprecated bridge file.
  // Tile definitions live in tile.js and map definitions live in maps.js.
  window.HEALER_TILE_DEFS = window.HEALER_TILE_DEFS || {};
  window.HEALER_TILE_MAPS = window.HEALER_TILE_MAPS || {};
})();
